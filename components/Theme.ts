import { createTheme } from "@mui/material";

/**規定テーマ */
const theme = createTheme({
  palette: {
    primary: {
      main: "#f2c849",
    },
    secondary: {
      main: "#f2b441",
    },
    text: { primary: "#402004" },
  },
});

export default theme;
